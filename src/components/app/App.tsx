import React from "react";
import { AppContainer, AppHeader, AppBody } from "./App.styles";

const App: React.FC = () => {
  return (
    <AppContainer>
      <AppHeader>AppHeader</AppHeader>
      <AppBody>AppBody</AppBody>
    </AppContainer>
  );
};

export default App;
